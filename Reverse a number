#include<stdio.h>
int n,i,rev,temp=0;
int main()
{
 printf("enter a number");
 scanf("%d",&n);
  for(i=1;n>0;i++)
   {
    rev=n%10;
    n=n/10;
    temp=temp*10+rev;
   }
 printf("the reversed number is %d",temp);
 return 0;
}
